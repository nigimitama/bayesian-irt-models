# Code Examples for Bayesian IRT model with Python

This repository includes sample codes using following packages:

- PyMC
- pystan

## setup

This project uses the [uv](https://docs.astral.sh/uv/) package.

To start the JupyterLab notebook editor, run the following command:

```sh
uv run --with jupyter jupyter lab
```
